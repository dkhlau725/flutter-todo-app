animation for flutter

use flare

body: new Center(
	child: FlareActor("anim/pumpkin.flr",
	alignment: Alignment.center,
	fit: BoxFit.contain,
	animation: "Untitled",
	), //FlareActor
      ), //Center


margin: space between widgets and other widgets (outside border)
padding: space between content and the border

color gradient

gradient: LinearGradient(
       	    begin: Alignment.topCenter,
	    end: Alignment.bottomCenter,
	    colors: [Colors.purple[50], Colors.purple[500]]
)			            ^gradient 	       ^actual